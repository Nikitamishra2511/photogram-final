  <h1>All Photos</h1>

   <% @list_of_photos.each do |photo| %>
     <div>
       <a href="/photos/<%= photo.id %>">
         <img src="<%= photo.image.url %>" alt="Photo" />
       </a>
       <p><%= photo.caption %></p>
     </div>
   <% end %>
